---
title: Colors
description: Box component supports multiple props for controlling an element's background, border, and text colors. Learn how to use Colors  in React with Axiom Design System.
openGraph:
  title: Colors – Styling Guide
  description: Box component supports multiple props for controlling an element's background, border, and text colors. Learn how to use Colors  in React with Axiom Design System.
---

import { theme } from "@optiaxiom/react";
import { Demo, Scale } from "@/components";


# Colors

`Box` component supports multiple props for controlling an element's background, border, and text colors.

You can also use the `--ax-colors-*` CSS variables directly in any CSS property.

## Documentation

### Using style props

<Demo component="styles/prop-usage" />

### Using CSS variables

<Demo component="styles/css-usage" />

### Dark mode

Semantic color names such as `bg.information`, `bg.success`, etc. support light/dark modes out of the box.

Set the [color-scheme](https://developer.mozilla.org/en-US/docs/Web/CSS/color-scheme) property on the root `<html>` element to switch to dark mode.

```css
:root {
  color-scheme: light dark;
}
```

## Color Tokens

### Text

<Scale
  maxH="full"
  mode="color"
  values={Object.fromEntries(
    Object.entries(theme.colors).filter(([name]) => name.startsWith("fg.")),
  )}
/>

### Background

<Scale
  maxH="full"
  mode="color"
  values={Object.fromEntries(
    Object.entries(theme.colors).filter(([name]) => name.startsWith("bg.")),
  )}
/>

### Border

<Scale
  maxH="full"
  mode="color"
  values={Object.fromEntries(
    Object.entries(theme.colors).filter(([name]) => name.startsWith("border.")),
  )}
/>
