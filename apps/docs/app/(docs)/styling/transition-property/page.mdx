import { Demo, demos } from "@/components/demo";
import { Scale } from "@/components/scale";

# Transition Property

Use `transition` prop to control the transition-property of an element.

<Scale hidePixels hidePreview values="transition" />

## Documentation

### Transition colors

Use `transition="colors"` to transition all color related properties.

<Demo component={demos["transition/colors-usage"]} />
