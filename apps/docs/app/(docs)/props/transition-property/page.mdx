import { Demo, Scale } from "@/components";

# Transition Property

Use `transition` prop to control the transition-property of an element.

<Scale values="transition" />

## Documentation

### Transition colors

Use `transition="colors"` to transition all color related properties.

<Demo component="transition/colors-usage" />
