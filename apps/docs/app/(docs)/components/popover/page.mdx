import { Demo, demos } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";

# Popover

Display arbitrary rich content inside a non-modal dialog triggered by a button.

## Documentation

### Usage

<Demo component={demos["popover/usage"]} />

### Controlled

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component={demos["popover/controlled-usage"]} />

### Example

<Demo component={demos["popover/example-usage"]} />

### Customize trigger

By default we use the `Button` component for the dialog trigger which accepts all of the existing button props.

We can also completely change the trigger by using `asChild` and passing our own component.

<Demo component={demos["popover/trigger-usage"]} />

## Props

### Popover

<PropsTable component={components["Popover"]} />

### PopoverTrigger

<PropsTable component={components["PopoverTrigger"]} />

### PopoverContent

<PropsTable component={components["PopoverContent"]} />

## Changelog

### 0.1.0

- Added component
