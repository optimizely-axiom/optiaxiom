import { Demo, Links, PropsTable } from "@/components";
import { Flex, Kbd } from "@optiaxiom/react";

# Popover

Display arbitrary rich content inside a non-modal dialog triggered by a button.

<Links component="popover" />

## Documentation

### Usage

<Demo component="popover/usage" />

### Anatomy

```tsx
import { Popover, PopoverContent, PopoverTrigger } from "@optiaxiom/react";

export default () => (
  <Popover>
    <PopoverTrigger />
    <PopoverContent />
  </Popover>
);
```

### Controlled

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component="popover/controlled-usage" />

### Example

<Demo component="popover/example-usage" />

### Customize trigger

By default we use the `Button` component for the dialog trigger which accepts all of the existing button props.

We can also completely change the trigger by using `asChild` and passing our own component.

<Demo component="popover/trigger-usage" />

## Props

### Popover

<PropsTable component="Popover" />

### PopoverTrigger

<PropsTable component="PopoverTrigger" />

### PopoverContent

<PropsTable component="PopoverContent" />

## Accessibility

### Keyboard interactions

| Key                                                                        | Description                                             |
| :------------------------------------------------------------------------- | :------------------------------------------------------ |
| <Flex flexDirection="row" gap="8"><Kbd>Space</Kbd> <Kbd>Enter</Kbd></Flex> | When focus is on `PopoverTrigger`, opens the popover.   |
| <Kbd>Esc</Kbd>                                                             | Closes the popover and moves focus to `PopoverTrigger`. |

## Changelog

### 0.1.0

- Added component
