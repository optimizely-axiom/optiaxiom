import { Cards, Demo, Links, PropsTable } from "@/components";
import { Alert } from "@optiaxiom/react";

# DropdownMenu

Display a dropdown menu.

<Links component="dropdown-menu" />

<Alert intent="danger" mt="16">
  **DropdownMenu** has been deprecated in favor of our new fully featured
  [Menu](../menu/) component that has a simpler API, allows filtering/selecting
  items, and includes built-in virtualization.
</Alert>

## Documentation

### Usage

Use the `DropdownMenu`, `DropdownMenuTrigger`, and `DropdownMenuContent` components to build out menus.

<Demo component="dropdown-menu/usage" />

### Controlled

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component="dropdown-menu/controlled-usage" meta="{18,26-27}" />

### Non modal usage

By default scrolling and interactions with outside elements is disabled when menu is open. Set `modal` prop to `false` in order to render a non-modal dropdown.

<Demo component="dropdown-menu/modal-usage" meta="/modal={false}/" />

### Trigger

#### Customize trigger

By default we use the `Button` component for the menu trigger which accepts all of the existing button props.

<Demo component="dropdown-menu/trigger-usage" meta="{15-17}" />

#### Ellipsis trigger

We can also use `asChild` to render a completely different component. We provide two built-in buttons:

- `AngleMenuButton` (default)
- `EllipsisMenuButton`

<Demo component="dropdown-menu/ellipsis-trigger-usage" meta="{15-17}" />

### Content

#### Menu position

Use the `align` and `side` props on `DropdownMenuContent` to change the default position of the dropdown.

<Demo
  component="dropdown-menu/position-usage"
  meta="/align={align}/ /side={side}/"
/>

### Item

#### Appearance

Use the `intent` prop to control the appearance of items.

<Demo meta='/intent="danger"/' component="dropdown-menu/appearance-usage" />

#### Description

Use the `description` prop to add secondary text to items.

<Demo meta="/description/" component="dropdown-menu/description-usage" />

#### Disabled items

Use the `disabled` prop on menu items to disable them.

<Demo meta="/disabled/" component="dropdown-menu/disabled-usage" />

#### Selection

Use the `onSelect` prop to handle selection of items.

<Demo component="dropdown-menu/select-usage" meta="{28,35}" />

#### Left and right addons

Use the `icon`, `addonBefore`, and `addonAfter` props to add icons, text, or any other element to the start or end of the menu items.

<Demo component="dropdown-menu/addon-usage" meta="{20-24,25,32-36,37}" />

### CheckboxItem

Use the `DropdownMenuCheckboxItem` component to render a checkbox and add the `checked` and `onCheckedChange` props to control the state.

<Demo component="dropdown-menu/checkbox-usage" />

### Label

Use the `DropdownMenuLabel` component to render a read-only label.

<Demo component="dropdown-menu/usage" meta="{5,17}" />

### Submenus

Use `DropdownMenuSub`, `DropdownMenuSubTrigger`, and `DropdownMenuSubContent` to render a submenus.

<Demo component="dropdown-menu/nested-usage" meta="{9-11,22-29}" />

### Dialogs inside menus

Combine dropdown menus with dialog to show alerts when selecting an item.

Make sure to use `AlertDialog` or `Dialog` in controlled mode since there is no actual trigger.

<Demo component="dropdown-menu/alert-dialog-usage" meta="{27,31}" />

### Related

<Cards items={["Menu"]} />

## Props

### DropdownMenu

<PropsTable component="DropdownMenu" />
### DropdownMenuTrigger
<PropsTable component="DropdownMenuTrigger" />
### DropdownMenuContent
<PropsTable component="DropdownMenuContent" />
### DropdownMenuItem
<PropsTable component="DropdownMenuItem" />
### DropdownMenuCheckboxItem
<PropsTable component="DropdownMenuCheckboxItem" />
### DropdownMenuLabel
<PropsTable component="DropdownMenuLabel" />
### DropdownMenuGroup
<PropsTable component="DropdownMenuGroup" />
### DropdownMenuSeparator
<PropsTable component="DropdownMenuSeparator" />
### DropdownMenuSub
<PropsTable component="DropdownMenuSub" />
### DropdownMenuSubTrigger
<PropsTable component="DropdownMenuSubTrigger" />
### DropdownMenuSubContent
<PropsTable component="DropdownMenuSubContent" />

## Changelog

### 1.6.0

- Deprecated component in favor of [Menu](../menu/)

### 0.1.0

- Added component
