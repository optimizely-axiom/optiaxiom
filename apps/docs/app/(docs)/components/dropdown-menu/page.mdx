import { Cards } from "@/components/cards";
import { Demo, demos } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";

# DropdownMenu

Display a dropdown menu.

## Documentation

### Usage

Use the `DropdownMenu`, `DropdownMenuTrigger`, and `DropdownMenuContent` components to build out menus.

<Demo component={demos["dropdown-menu/usage"]} />

### Controlled

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component={demos["dropdown-menu/controlled-usage"]} meta="{16,24-25}" />

### Non modal usage

By default scrolling and interactions with outside elements is disabled when menu is open. Set `modal` prop to `false` in order to render a non-modal dropdown.

<Demo component={demos["dropdown-menu/modal-usage"]} meta="/modal={false}/" />

### Trigger

#### Customize trigger

By default we use the `Button` component for the menu trigger which accepts all of the existing button props.

<Demo component={demos["dropdown-menu/trigger-usage"]} meta="{15-17}" />

### Content

#### Menu position

Use the `align` and `side` props on `DropdownMenuContent` to change the default position of the dropdown.

<Demo
  component={demos["dropdown-menu/position-usage"]}
  meta="/align={align}/ /side={side}/"
/>

### Item

#### Appearance

Use the `intent` prop to control the appearance of items.

<Demo
  meta='/intent="danger"/'
  component={demos["dropdown-menu/appearance-usage"]}
/>

#### Description

Use the `description` prop to add secondary text to items.

<Demo
  meta="/description/"
  component={demos["dropdown-menu/description-usage"]}
/>

#### Disabled items

Use the `disabled` prop on menu items to disable them.

<Demo meta="/disabled/" component={demos["dropdown-menu/disabled-usage"]} />

#### Selection

Use the `onSelect` prop to handle selection of items.

<Demo component={demos["dropdown-menu/select-usage"]} meta="{26,33}" />

#### Left and right addons

Use the `icon`, `addonBefore`, and `addonAfter` props to add icons, text, or any other element to the start or end of the menu items.

<Demo
  meta="{20-24,25,32-36,37}"
  component={demos["dropdown-menu/addon-usage"]}
/>

### Label

Use the `DropdownMenuLabel` component to render a read-only label.

<Demo meta="{5,17}" component={demos["dropdown-menu/usage"]} />

### Dialogs inside menus

Combine dropdown menus with dialog to show alerts when selecting an item.

Make sure to enable `asChild` prop on `AlertDialogTrigger` and call `event.preventDefault()` in the item `onSelect` handler.

<Demo component={demos["dropdown-menu/alert-dialog-usage"]} meta="{25,29}" />

### Related

<Cards items={["Autocomplete", "Select"]} />

## Props

### DropdownMenu

<PropsTable component={components["DropdownMenu"]} />
### DropdownMenuTrigger
<PropsTable component={components["DropdownMenuTrigger"]} />
### DropdownMenuContent
<PropsTable component={components["DropdownMenuContent"]} />
### DropdownMenuItem
<PropsTable component={components["DropdownMenuItem"]} />
### DropdownMenuLabel
<PropsTable component={components["DropdownMenuLabel"]} />
### DropdownMenuGroup
<PropsTable component={components["DropdownMenuGroup"]} />
### DropdownMenuSeparator
<PropsTable component={components["DropdownMenuSeparator"]} />
### DropdownMenuSub
<PropsTable component={components["DropdownMenuSub"]} />
### DropdownMenuSubTrigger
<PropsTable component={components["DropdownMenuSubTrigger"]} />
### DropdownMenuSubContent
<PropsTable component={components["DropdownMenuSubContent"]} />

## Changelog

### 0.1.0

- Added component
