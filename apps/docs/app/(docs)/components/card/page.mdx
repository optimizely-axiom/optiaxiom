import { Demo, Links, PropsTable } from "@/components";
import { App as AnatomyUsage } from "@/demos/card/anatomy-usage/App";
import { Alert } from "@optiaxiom/react";

# Card

Generic container for grouping related components together.

<Links component="card" />

## Documentation

### Usage

<Demo component="card/usage" />

### Anatomy

```tsx
import {
  Card,
  CardAction,
  CardCheckbox,
  CardFooter,
  CardHeader,
  CardImage,
  CardLink,
  CardPreview,
} from "@optiaxiom/react";

export default () => (
  <Card>
    <CardPreview>
      <CardImage />
      <CardCheckbox />
      <CardAction />
    </CardPreview>

    <CardHeader>
      <CardLink />
    </CardHeader>

    <CardFooter />
  </Card>
);
```

### Structure

Card has a basic structure containing the following parts:

- `CardPreview`: An optional media/thumbnail preview.
- `CardHeader`: Contains the title and an optional short description along with slots for icons, actions, etc. on left/right side of the title.
- `CardFooter`: Contains secondary information and sticks to the bottom of the card.

`CardHeader` is the only required element - and all other elements are optional.

<AnatomyUsage />

### Header

Use the `CardHeader` component to add a title to cards.

<Demo component="card/title-usage" />

#### Description

Use the `description` prop on `CardHeader` to add short descriptions to cards.

<Demo component="card/description-usage" />

#### Addons

Use the `addonAfter` prop on `CardHeader` to add menus and other secondary actions after the title.

<Demo component="card/addon-after-usage" />

Use the `addonBefore` prop on `CardHeader` to add icons or other visuals before the title.

<Demo component="card/addon-before-usage" />

#### Truncate

We can use the [Text](../text/) component to truncate header titles.

<Demo component="card/truncate-usage" />

### Links

Use the `CardLink` component to render links and make the whole card clickable.

<Demo component="card/link-usage" meta="{35}" />

#### Tooltip

We can combine this with the [Text](../text/) and [Tooltip](../tooltip/) component to truncate header titles while also showing the full title in a tooltip.

Use the `auto` prop on `Tooltip` to only show a tooltip if the text is actually clipped.

<Demo component="card/link-truncate-usage" />

### Preview

Use the `CardPreview` component to add a preview section for cards.

<Demo component="card/preview-usage" />

We can also use the `CardImage` component to add thumbnail images for cards.

<Demo component="card/preview-image-usage" meta="{17-23}" />

### Overlay

Use the `addonTopLeft`/`addonTopRight`/`addonBottomRight`/`addonBottomLeft` props on `CardPreview` to add checkbox and other tertiary actions to cards.

<Demo
  component="card/overlay-usage"
  meta="/addonTopLeft/ /addonTopRight/ /addonBottomRight/ /addonBottomLeft/"
/>

Include the `CardAction` component if you'd like the checkbox to be hidden until hovered or interacted with.

<Alert intent="warning" mt="16">
  Hover over the card to see the checkbox:
</Alert>

<Demo component="card/overlay-action-usage" meta="{17,19,40,42,45,47}" />

### Sortable

We can combine cards with the [Sortable](../sortable/) component to build a sortable list of cards.

<Demo component="sortable/card-usage" />

## Props

### Card

<PropsTable component="Card" />

### CardAction

<PropsTable component="CardAction" />

### CardCheckbox

<PropsTable component="CardCheckbox" />

### CardFooter

<PropsTable component="CardFooter" />

### CardHeader

<PropsTable component="CardHeader" />

### CardImage

<PropsTable component="CardImage" />

### CardLink

<PropsTable component="CardLink" />

### CardPreview

<PropsTable component="CardPreview" />

## Changelog

### 1.6.0

- Moved component out of **Alpha**.

  ```jsx
  // Before
  import { Card } from "@optiaxiom/react/unstable";

  // After
  import { Card } from "@optiaxiom/react";
  ```

### 1.5.0

- Renamed `CardOverflow` component to `CardPreview`:

  ```jsx
  // Before
  <Card>
    <CardOverflow />
  </Card>

  // After
  <Card>
    <CardPreview />
  </Card>
  ```

- Removed `CardContent` component

- Combined `CardTitle` and `CardDescription` into one `CardHeader` component:

  ```jsx
  // Before
  <Card>
    <CardTitle>Sample Title</CardTitle>
    <CardDescription>Sample Description</CardDescription>
  </Card>

  // After
  <Card>
    <CardHeader description="Sample Description">
      Sample Title
    </CardHeader>
  </Card>
  ```

### 0.12.1

- Added component
