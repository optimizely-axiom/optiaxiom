import { Cards } from "@/components/cards";
import { Demo, demos } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";

# Text

Display body or any other form of text. By default it outputs the `<p>` paragraph element.

## Documentation

### Usage

<Demo component={demos["text/usage"]} />

### Formatting

<Demo component={demos["text/formatting-usage"]} />

### Truncate

Use the `truncate` prop to truncate the text and add an ellipsis at the end.

<Demo component={demos["text/truncate-usage"]} />

### Line clamp

Use the `lineClamp` prop to truncate the text at specific number of lines.

<Demo component={demos["text/line-clamp-usage"]} />

### Composition

`Text` component uses the [Radix Composition](https://www.radix-ui.com/primitives/docs/guides/composition) approach and accepts an `asChild` prop. By default it renders a `<p>` element but when `asChild` is enabled it will forward all of its own props to its child and only render the child element instead.

<Demo component={demos["text/composition-usage"]} />

### Related

<Cards items={["Heading"]} />

## Props

### Text

<PropsTable component={components["Text"]} />

## Changelog

### 0.1.0

- Added component
