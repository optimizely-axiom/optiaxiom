import { Demo, KeyboardTable, Links, PropsTable } from "@/components";
import { Kbd } from "@optiaxiom/react";

# Calendar

Base date picker component.

<Links component="calendar" />

## Documentation

### Usage

<Demo component="calendar/usage" />

### Localization

The calendar is localized to the browser locale (via `navigator.language`) by default, which controls the captions, weekday headers, and which day the week starts on. Override the locale for the whole app by passing the `locale` prop to [AxiomProvider](../axiom-provider/#locale).

## Props

### Calendar

<PropsTable component="Calendar" />

## Accessibility

### Keyboard interactions

<KeyboardTable
  interactions={[
    {
      keys: "Space",
      description: "Selects the focused day.",
    },
    {
      keys: "Enter",
      description: "Selects the focused day.",
    },
    {
      keys: ["ArrowUp", "ArrowDown", "ArrowRight", "ArrowLeft"],
      description: "Navigate between days in the calendar.",
    },
    {
      keys: ["PageUp", "PageDown"],
      description: "Navigate between months in the calendar.",
    },
    {
      keys: [
        { key: "PageUp", modifiers: "shift" },
        { key: "PageDown", modifiers: "shift" },
      ],
      description: "Navigate between years in the calendar.",
    },
  ]}
/>

## Changelog

### 3.1.0

- Added [localization](#localization) support

### 0.12.1

- Added component
