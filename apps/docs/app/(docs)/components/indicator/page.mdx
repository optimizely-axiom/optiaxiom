import { Demo } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";
import {
  AppearanceUsage,
  ContentUsage,
  DisabledUsage,
  Usage,
} from "@/demos/indicator";

# Indicator

Display a badge at the corner of another element.

## Documentation

### Usage

<Demo component={Usage} />

### Content

Use the `content` prop to set the content of the badge.

<Demo component={ContentUsage} />

### Appearance

Use the `intent` and `variant` props to fine tune the appearance of the badge.

<Demo component={AppearanceUsage} />

### Hide indicator

Use the `disabled` prop to hide the badge.

<Demo component={DisabledUsage} meta="/disabled={disabled}/" />

## Props

### Indicator

<PropsTable component={components["Indicator"]} />

## Changelog

### 0.1.0

- Added component
