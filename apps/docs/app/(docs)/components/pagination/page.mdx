import { Cards } from "@/components/cards";
import { Demo } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";
import { ControlledUsage, Usage } from "@/demos/pagination";

# Pagination

Display active page and navigate between multiple pages.

## Documentation

### Usage

<Demo component={Usage} />

### Controlled

Use the `page` and `defaultPage` props to toggle between controlled and uncontrolled usage. And combine it with `onPageChange` to listen for changes to the state.

<Demo component={ControlledUsage} />

### Related

<Cards items={["Table"]} />

## Props

### Pagination

<PropsTable component={components["Pagination"]} />

## Changelog

### 0.1.0

- Added component
