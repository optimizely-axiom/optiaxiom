import { Demo, demos } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";

# Tooltip

Popup with brief information shown when user interacts with an element using keyboard focus or mouse hover.

## Documentation

### Usage

<Demo component={demos["tooltip/usage"]} />

### Controlled

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component={demos["tooltip/controlled-usage"]} />

### Delay

Use the `delayDuration` prop to control how long to wait to open the tooltip after mouse hover.

<Demo component={demos["tooltip/delay-usage"]} />

### Placement

Use the `side` and `align` props to control where the tooltip is placed by default.

<Demo component={demos["tooltip/placement-usage"]} />

## Props

### Tooltip

<PropsTable component={components["Tooltip"]} />
### TooltipProvider
<PropsTable component={components["TooltipProvider"]} />

## Changelog

### 0.1.0

- Added component
