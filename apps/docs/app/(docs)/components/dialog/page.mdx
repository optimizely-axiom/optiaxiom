import { Cards } from "@/components/cards";
import { Demo, demos } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";
import { Alert } from "@optiaxiom/react";
import { Callout } from "nextra/components";

# Dialog

Display a modal dialog box.

## Documentation

### Usage

<Demo component={demos["dialog/usage"]} />

### Controlled

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component={demos["dialog/controlled-usage"]} meta="{17}" />

### Sizes

Use the `size` prop to change the size of the dialog box.

<Demo meta="/size={size}/" component={demos["dialog/size-usage"]} />

### Description

You can add accessible description to the modal using the `description` prop on `DialogHeader` component.

<Demo meta="{16}" component={demos["dialog/description-usage"]} />

### Customize trigger

By default we use the `Button` component for the dialog trigger which accepts all of the existing button props.

We can also completely change the trigger by using `asChild` and passing our own component.

<Demo component={demos["dialog/trigger-usage"]} meta="{16-20}" />

### Form dialogs

Wrap `DialogBody` and `DialogFooter` with the `DialogForm` component to use forms in dialogs.

<Demo component={demos["dialog/form-usage"]} meta="{31-41,62}" />

### Nested dialogs

Combine dialogs to show nested dialogs.

<Demo component={demos["dialog/nested-usage"]} />

### Nested alert dialogs

Combine `Dialog` with `AlertDialog` to show nested confirmation dialogs.

Use the `onOpenChange` handler to trigger the nested callback instead of an explicit trigger.

<Alert intent="warning" mt="16">
  Type something into the text field and then attempt to close the dialog:
</Alert>

<Demo component={demos["dialog/alert-dialog-usage"]} />

### Related

<Cards items={["AlertDialog"]} />

## Props

### Dialog

<PropsTable component={components["Dialog"]} />
### DialogTrigger
<PropsTable component={components["DialogTrigger"]} />
### DialogContent
<PropsTable component={components["DialogContent"]} />
### DialogHeader
<PropsTable component={components["DialogHeader"]} />
### DialogForm
<PropsTable component={components["DialogForm"]} />
### DialogBody
<PropsTable component={components["DialogBody"]} />
### DialogFooter
<PropsTable component={components["DialogFooter"]} />
### DialogClose
<PropsTable component={components["DialogClose"]} />

## Changelog

### 0.1.0

- Added component
