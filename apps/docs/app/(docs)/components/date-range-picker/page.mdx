import { Demo, KeyboardTable, Links, PropsTable } from "@/components";
import { Kbd } from "@optiaxiom/react";

# DateRangePicker

Calendar popover that lets user pick date ranges.

<Links component="date-range-picker" />

## Documentation

### Usage

<Demo component="date-range-picker/usage" />

### Anatomy

```tsx
import {
  DateRangePicker,
  DateRangePickerContent,
  DateRangePickerTrigger,
} from "@optiaxiom/react";

export default () => (
  <DateRangePicker>
    <DateRangePickerTrigger />
    <DateRangePickerContent />
  </DateRangePicker>
);
```

### Controlled

Use the `value` and `defaultValue` props to toggle between controlled and uncontrolled usage. And combine it with `onValueChange` to listen for changes to the state.

<Demo component="date-range-picker/controlled-usage" />

### Addons

Use the `addonBefore` and `addonAfter` prop on `DateRangePickerContent` to add content to either side of the calendar.

<Demo component="date-range-picker/addon-usage" />

## Props

### DateRangePicker

<PropsTable component="DateRangePicker" />

### DateRangePickerTrigger

<PropsTable component="DateRangePickerTrigger" />

### DateRangePickerContent

<PropsTable component="DateRangePickerContent" />

## Accessibility

### Keyboard interactions

<KeyboardTable
  interactions={[
    {
      keys: "Space",
      description:
        "When focus is on `DateRangePickerTrigger`, opens the calendar and focuses the selected day (or today's date). When focus is on a day, selects the focused day.",
    },
    {
      keys: "Enter",
      description:
        "When focus is on `DateRangePickerTrigger`, opens the calendar and focuses the selected day (or today's date). When focus is on a day, selects the focused day.",
    },
    {
      keys: ["ArrowUp", "ArrowDown", "ArrowRight", "ArrowLeft"],
      description: "Navigate between days in the calendar.",
    },
    {
      keys: ["PageUp", "PageDown"],
      description: "Navigate between months in the calendar.",
    },
    {
      keys: [
        { key: "PageUp", modifiers: "shift" },
        { key: "PageDown", modifiers: "shift" },
      ],
      description: "Navigate between years in the calendar.",
    },
    {
      keys: "Esc",
      description:
        "Closes the calendar popover and moves focus back to `DateRangePickerTrigger`.",
    },
  ]}
/>

## Changelog

### 1.4.0

- Moved component out of **Alpha**.

  ```jsx
  // Before
  import { DateRangePicker } from "@optiaxiom/react/unstable";

  // After
  import { DateRangePicker } from "@optiaxiom/react";
  ```

### 0.13.10

- Added component
