import { Cards } from "@/components/cards";
import { Demo, demos } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";

# Badge

Use to emphasize a status, count, state or value.

## Documentation

### Usage

<Demo component={demos["badge/usage"]} />

### Appearance

Use the `intent` and `variant` props to fine tune the appearance of the badge.

<Demo component={demos["badge/appearance-usage"]} />

### Badge as links

Set `asChild` to `true` and wrap children in an `<a>` element to render badge as links.

<Demo component={demos["badge/link-usage"]} />

### Related

<Cards items={["Indicator"]} />

## Props

### Badge

<PropsTable component={components["Badge"]} />

## Changelog

### 0.1.0

- Added component
