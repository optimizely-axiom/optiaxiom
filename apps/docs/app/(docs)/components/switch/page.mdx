import { Cards } from "@/components/cards";
import { Demo, demos } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";

# Switch

Control to allow toggling between checked and not checked state.

## Documentation

### Usage

<Demo component={demos["switch/usage"]} />

### Label

The `children` prop is used as the label of the switch control.

<Demo component={demos["switch/label-usage"]} />

### Sizes

Use the `size` prop to control the size of the switch control.

<Demo component={demos["switch/size-usage"]} />

### Disabled state

Enable the `disabled` prop to toggle the disabled state of the input field.

<Demo component={demos["switch/disabled-usage"]} />

### Controlled

Use the `checked` and `defaultChecked` props to toggle between controlled and uncontrolled usage. And combine it with `onCheckedChange` to listen for changes to the state.

<Demo component={demos["switch/controlled-usage"]} />

### Related

<Cards items={["Checkbox", "RadioGroup"]} />

## Props

### Switch

<PropsTable component={components["Switch"]} />

## Changelog

### 0.1.0

- Added component
