import { Demo } from "@/components/demo";
import { PropsTable } from "@/components/props-table";

# Disclosure

An interactive component which expands/collapses a panel.

## Documentation

### Usage

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component="disclosure/usage" />

### Indicator position

Use the `chevronPosition` prop on `DisclosureTrigger` to control which side the chevron is placed.

<Demo
  meta="/chevronPosition={chevronPosition}/"
  component="disclosure/chevron-position-usage"
/>

### Addons

Use the `addonBefore` and `addonAfter` prop to add icons, text, or any other interactive elements to the start or end of the trigger.

<Demo component="disclosure/addon-usage" />

## Props

### Disclosure

<PropsTable component="Disclosure" />

### DisclosureTrigger

<PropsTable component="DisclosureTrigger" />

### DisclosureContent

<PropsTable component="DisclosureContent" />

## Changelog

### 0.1.0

- Added component
