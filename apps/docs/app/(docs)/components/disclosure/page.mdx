import { Demo } from "@/components/demo";
import { components, PropsTable } from "@/components/props-table";
import { ChevronPositionUsage, Usage } from "@/demos/disclosure";

# Disclosure

An interactive component which expands/collapses a panel.

## Documentation

### Usage

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component={Usage} />

### Description

Use the `chevronPosition` prop on `DisclosureTrigger` to control which side the chevron is placed.

<Demo
  meta="/chevronPosition={chevronPosition}/"
  component={ChevronPositionUsage}
/>

## Props

### Disclosure

<PropsTable component={components["Disclosure"]} />

### DisclosureTrigger

<PropsTable component={components["DisclosureTrigger"]} />

### DisclosureContent

<PropsTable component={components["DisclosureContent"]} />

## Changelog

### 0.1.0

- Added component
