<ax-layout />

Use `Flex` component to stack items vertically or horizontally.

## Usage

<ax-demo name="flex-usage" />

## Direction

Use `flexDirection="row"` or `flexDirection="horizontal"` to stack items horizontally:

<ax-demo name="flex-horizontal-usage" />

Use `flexDirection="column"` or `flexDirection="vertical"` to stack items vertically:

<ax-demo name="flex-vertical-usage" />
