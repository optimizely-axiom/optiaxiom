# Search

Basic search input field with clear button.

## Documentation

### Usage

The `Search` component extends the `Input` component with some additional decorators.

<ax-demo name="search/usage" />

### Disabled state

Enable the `disabled` prop to toggle the disabled state of the input field.

<ax-demo name="search/disabled-usage" />

### Controlled

Use the `value` and `defaultValue` props to toggle between controlled and uncontrolled usage. And combine it with `onChange` to listen for changes to the input field.

<ax-demo name="search/controlled-usage" />

## Props

<ax-props-table component="Search" extends="Input" />

## Changelog

### 0.1.0

- Added component
