# Input

Basic text field for capturing user input.

## Basic usage

### Usage

<ax-demo name="input/usage" />

### Sizes

Use the `size` prop to control the size of the input field.

<ax-demo name="input/size-usage" />

### Error state

Enable the `error` prop to display the error state of the input field.

<ax-demo name="input/error-usage" />

### Disabled state

Enable the `disabled` prop to toggle the disabled state of the input field.

<ax-demo name="input/disabled-usage" />

### Read-only state

Enable the `readOnly` prop to display the read-only state of the input field.

<ax-demo name="input/readonly-usage" />

## Decorators

### Left and right decorators

Use the `startDecorator` and `endDecorator` prop to add icons, text, or any other element to the start or end of the input.

<ax-demo name="input/decorator-usage" />

## Props

<ax-props-table component="Input" />

<ax-props-table component="InputBase" />

## Changelog

### 0.1.0

- Added component
