import { Demo } from "@/components/demo";
import { PropsTable } from "@/components/props-table";
import {
  AddonUsage,
  AppearanceUsage,
  ControlledUsage,
  DescriptionUsage,
  DisabledUsage,
  ModalUsage,
  PositionUsage,
  SelectUsage,
  TriggerCompositionUsage,
  TriggerUsage,
  Usage,
} from "@/demos/dropdown-menu";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@optiaxiom/react";

# DropdownMenu

Display a dropdown menu.

## Documentation

### Usage

Use the `DropdownMenu`, `DropdownMenuTrigger`, and `DropdownMenuContent` components to build out menus.

<Demo component={Usage} />

### Controlled

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<Demo component={ControlledUsage} />

### Non modal usage

By default scrolling and interactions with outside elements is disabled when menu is open. Set `modal` prop to `false` in order to render a non-modal dropdown.

<Demo component={ModalUsage} />

## Trigger

### Customize trigger

By default we use the `Button` component for the menu trigger which accepts all of the existing button props.

<Demo component={TriggerUsage} />

### Composition

We can also completely change the trigger by using `asChild` and passing our own component.

<Demo component={TriggerCompositionUsage} />

## Content

### Menu position

Use the `align` and `side` props on `DropdownMenuContent` to change the default position of the dropdown.

<Demo component={PositionUsage} />

## Item

### Appearance

Use the `colorScheme` prop to control the appearance of items.

<Demo meta='/colorScheme="danger"/' component={AppearanceUsage} />

### Description

Use the `description` prop to add secondary text to items.

<Demo meta="/description/" component={DescriptionUsage} />

### Disabled items

Use the `disabled` prop on menu items to disable them.

<Demo meta="/disabled/" component={DisabledUsage} />

### Label

Use the `DropdownMenuLabel` component to render a read-only label.

<Demo meta="/DropdownMenuLabel/" component={Usage} />

### Selection

Use the `onSelect` prop to handle selection of items.

<Demo component={SelectUsage} />

### Left and right addons

Use the `addonBefore` and `addonAfter` prop to add icons, text, or any other element to the start or end of the menu items.

<Demo meta="/addonBefore/ /addonAfter/" component={AddonUsage} />

## Props

<PropsTable component={DropdownMenu} base={false} />

<PropsTable component={DropdownMenuContent} />

<PropsTable component={DropdownMenuItem} />

<PropsTable component={DropdownMenuLabel} />

<PropsTable component={DropdownMenuSeparator} />

<PropsTable component={DropdownMenuTrigger} />

## Changelog

### 0.1.0

- Added component
