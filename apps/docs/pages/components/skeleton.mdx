import { Demo } from "@/components/demo";
import {
  ChildrenUsage,
  DimensionUsage,
  ExampleUsage,
  Usage,
} from "@/demos/skeleton";

# Skeleton

Display placeholder content while data is loading.

## Documentation

### Usage

<Demo component={Usage} />

### Children

Use `children` to insert things such as icons or graphics.

<Demo component={ChildrenUsage} />

### Dimensions

Skeleton is automatically sized according to it's children so we can combine it with `Text` or `Heading` component to achieve correctly sized textual placeholders.

<Demo component={DimensionUsage} />

### Example

<Demo component={ExampleUsage} />

## Props

<ax-props-table component="Skeleton" />

## Changelog

### 0.1.0

- Added component
