# AlertDialog

Display a modal with important content that expects confirmation from the user.

## Documentation

### Usage

Use the `open` and `defaultOpen` props to toggle between controlled and uncontrolled usage. And combine it with `onOpenChange` to listen for changes to the state.

<ax-demo name="alert-dialog/usage" />

### Appearance

Use the `appearance` prop to change the action button's appearance.

<ax-demo
  meta="/appearance={appearance}/"
  name="alert-dialog/appearance-usage"
/>

### Sizes

Use the `size` prop to change the size of the dialog box.

<ax-demo meta="/size={size}/" name="alert-dialog/size-usage" />

## Props

<ax-props-table component="AlertDialog" />

<ax-props-table component="AlertDialogTrigger" extends="Button" />

<ax-props-table component="AlertDialogContent" />

<ax-props-table component="AlertDialogTitle" />

<ax-props-table component="AlertDialogDescription" />

<ax-props-table component="AlertDialogFooter" />

<ax-props-table component="AlertDialogCancel" extends="Button" />

<ax-props-table component="AlertDialogAction" extends="Button" />

## Changelog

### 0.1.0

- Added component
