<ax-layout />

Use `Grid` component to place items in a grid using equal width columns.

## Usage

Use `cols` prop to create equally sized columns in a grid layout.

<ax-demo name="grid/usage" />

## Grid Columns

<ax-demo meta='/colSpan="2"/' name="grid-column/span-usage" />
