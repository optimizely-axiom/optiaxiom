# Switch

Control to allow toggling between checked and not checked state.

## Documentation

### Usage

<ax-demo name="switch/usage" />

### Label

The `children` prop is used as the label of the switch control.

<ax-demo name="switch/label-usage" />

### Sizes

Use the `size` prop to control the size of the switch control.

<ax-demo name="switch/size-usage" />

### Disabled state

Enable the `disabled` prop to toggle the disabled state of the input field.

<ax-demo name="switch/disabled-usage" />

### Controlled

Use the `checked` and `defaultChecked` props to toggle between controlled and uncontrolled usage. And combine it with `onCheckedChange` to listen for changes to the state.

<ax-demo name="switch/controlled-usage" />

## Decorators

### Bottom decorators

Use the `endDecorator` prop to add helper text or any other element after the switch.

<ax-demo name="switch/decorator-usage" />

## Props

<ax-props-table component="Switch" />

## Changelog

### 0.1.0

- Added component
