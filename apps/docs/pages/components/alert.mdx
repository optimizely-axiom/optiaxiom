<ax-layout />

Keeps users informed of important and sometimes time-sensitive changes.

## Basic usage

### Usage

<ax-demo name="alert/usage" />

### Appearance

Use the `colorScheme` and `variant` prop to select between the different alert types.

<ax-demo name="alert/appearance-usage" />

### Close button

Use the `onClose` prop to show a close button to the top right of the alert.

<ax-demo name="alert/close-usage" />

### Banner

Use the `solid` variant prop to display site-wide banner messages.

<ax-demo name="alert/banner-usage" />
