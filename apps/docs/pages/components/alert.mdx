import { Cards } from "@/components/cards";
import { Demo } from "@/components/demo";
import { PropsTable } from "@/components/props-table";
import { AppearanceUsage, CloseUsage, Usage } from "@/demos/alert";
import { Alert, AlertDescription, AlertTitle } from "@optiaxiom/react";

# Alert

Keeps users informed of important and sometimes time-sensitive changes.

## Documentation

### Usage

<Demo component={Usage} />

### Appearance

Use the `intent` prop to select between the different alert types.

<Demo component={AppearanceUsage} />

### Close button

Use the `onClose` prop to show a close button to the top right of the alert.

<Demo component={CloseUsage} />

### Related

<Cards items={["Banner", "Toast"]} />

## Props

<PropsTable component={Alert} />

<PropsTable component={AlertTitle} />

<PropsTable component={AlertDescription} />

## Changelog

### 0.1.0

- Added component
