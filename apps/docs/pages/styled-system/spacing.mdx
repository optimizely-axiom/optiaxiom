import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Spacing

`Box` component supports multiple props for controlling an element's margin and padding.

Additionally the `Grid` and `Stack` components support `gap` prop for controlling gutters between items.

## Scale

The spacing scale includes numeric values and also convenient t-shirt sizes.

<Scale values={tokens.spacing} />

## Gap

### Usage

Use the space scale to set the gap of elements.

<ax-demo name="spacing/gap/usage" />

## Margin

### Usage

Use the space scale to set the margin of elements.

<ax-demo name="spacing/margin/usage" />

### Multiple sides

<ax-demo name="spacing/margin/multiple-usage" />

### Single side

<ax-demo name="spacing/margin/single-usage" />

### Negative

<ax-demo name="spacing/margin/negative-usage" />

## Padding

### Usage

Use the space scale to set the padding of elements.

<ax-demo name="spacing/padding/usage" />

### Multiple sides

<ax-demo name="spacing/padding/multiple-usage" />

### Single side

<ax-demo name="spacing/padding/single-usage" />
