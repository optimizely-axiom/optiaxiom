import { Scale } from "@/components/scale";
import { sprinkles } from "@optiaxiom/react";
import { Callout } from "nextra/components";

# Flex Shrink

Use `shrink` prop to control the flex-shrink CSS property of an element.

<Scale
  hidePixels
  hidePreview
  valueLabel="Value"
  values={sprinkles.values.flexShrink}
/>

<Callout emoji="🚨">
  This page is a stub. Help us expand it by contributing!
</Callout>
