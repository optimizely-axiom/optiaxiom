import { Scale } from "@/components/scale";
import { sprinkles } from "@optiaxiom/react";
import { Callout } from "nextra/components";

# Overflow

Use `overflow` prop to control the overflow CSS property of an element.

<Scale
  hidePixels
  hidePreview
  valueLabel="Value"
  values={sprinkles.values.overflow}
/>

<Callout emoji="🚨">
  This page is a stub. Help us expand it by contributing!
</Callout>
