import { Scale } from "@/components/scale";
import { sprinkles } from "@optiaxiom/react";
import { Callout } from "nextra/components";

# Flex Wrap

Use `flexWrap` prop to control the flex-wrap CSS property of an element.

<Scale
  hidePixels
  hidePreview
  valueLabel="Value"
  values={sprinkles.values.flexWrap}
/>

<Callout emoji="🚨">
  This page is a stub. Help us expand it by contributing!
</Callout>
