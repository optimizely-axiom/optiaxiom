import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Ring Width

Use `ring` prop to create outline rings with box-shadows.

<Scale hidePixels values={tokens.ringWidth} />

## Usage

<ax-demo name="ring-width/usage" />

## Focus rings

<ax-demo name="ring-width/focus-usage" />
