import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Outline Width

Use `outline` prop to control the outline width of an element.

<Scale hidePixels values={tokens.outlineWidth} />

## Usage

<ax-demo name="outline-width/usage" />

## Focus outlines

<ax-demo name="outline-width/focus-usage" />
