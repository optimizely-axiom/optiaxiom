import { Scale } from "@/components/scale";
import { sprinkles } from "@optiaxiom/react";
import { Callout } from "nextra/components";

# Flex Direction

Use `flexDirection` prop to control the flex-direction CSS property of an element.

<Scale
  hidePixels
  hidePreview
  valueLabel="Value"
  values={sprinkles.values.flexDirection}
/>

<Callout emoji="🚨">
  This page is a stub. Help us expand it by contributing!
</Callout>
