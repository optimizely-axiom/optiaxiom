import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Font Family

Use `fontFamily` prop to control the font-family of an element.

<Scale hidePixels hidePreview values={tokens.fontFamily} />

## Usage

<ax-demo name="font/family-usage" />
