import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Max-Height

Use `maxH` prop to control the max-height of an element.

<Scale hidePreview values={tokens.maxSize} />

## Basic usage

### Usage

<ax-demo name="sizing/max-height/usage" />
