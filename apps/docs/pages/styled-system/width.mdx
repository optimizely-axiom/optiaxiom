import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Width

`Box` component supports `width` prop for controlling an element's width.

<Scale values={tokens.size} />

## Basic usage

### Usage

<ax-demo name="width/usage" />

### Percentage

<ax-demo name="width/percentage-usage" />
