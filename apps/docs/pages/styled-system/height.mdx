import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Height

Use `h` prop to control the height of an element.

<Scale values={tokens.size} />

## Documentation

### Usage

<ax-demo name="height/usage" />
