import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Border Radius

`Box` component supports the `rounded` prop for controlling the border radius of an element.

<Scale values={tokens.borderRadius} />

## Usage

<ax-demo name="border-radius/usage" />

### Individual sides

<ax-demo name="border-radius/sides-usage" />

### Individual corners

<ax-demo name="border-radius/corners-usage" />
