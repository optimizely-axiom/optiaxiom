import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Size

Use `size` prop to control both the width and height of an element.

<Scale values={tokens.size} />

## Documentation

### Usage

<ax-demo name="size/usage" />

### Percentage

<ax-demo name="size/percentage-usage" />

### Reset

<ax-demo name="size/reset-usage" />
