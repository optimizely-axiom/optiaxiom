import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

import { Demo } from "@/components/demo";
import { MultipleUsage, SingleUsage, Usage } from "@/demos/padding";

# Padding

`Box` component supports multiple props for controlling an element's padding.

<Scale values={tokens.spacing} />

## Documentation

### Usage

Use the space scale to set the padding of elements.

<Demo component={Usage} />

### Multiple sides

<Demo component={MultipleUsage} />

### Single side

<Demo component={SingleUsage} />
