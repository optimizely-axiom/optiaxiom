import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

import { Demo } from "@/components/demo";
import { InnerUsage, NoneUsage, Usage } from "@/demos/box-shadow";

# Box Shadow

Use `shadow` prop to control the box shadow of an element.

<Scale hidePixels hidePreview values={tokens.boxShadow} />

## Documentation

### Usage

<Demo component={Usage} />

### Inner shadow

<Demo component={InnerUsage} />

### Removing the shadow

<Demo component={NoneUsage} />
