import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Line Height

Use `leading` prop to control the line-height of an element.

<Scale hidePixels hidePreview valueLabel="Value" values={tokens.lineHeight} />

## Usage

<ax-demo name="font/leading-usage" />
