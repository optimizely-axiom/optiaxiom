import { Scale } from "@/components/scale";
import { sprinkles } from "@optiaxiom/react";

# Grid Template Columns

Use `cols` prop to control the grid-template-columns CSS property of an element.

<Scale
  hidePixels
  hidePreview
  valueLabel="Value"
  values={sprinkles.values.gridTemplateColumns}
/>

## Usage

Use `cols` prop to create equally sized columns in a grid layout.

<ax-demo name="grid-template-columns/usage" />
