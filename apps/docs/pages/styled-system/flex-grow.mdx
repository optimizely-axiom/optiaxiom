import { Scale } from "@/components/scale";
import { sprinkles } from "@optiaxiom/react";
import { Callout } from "nextra/components";

# Flex Grow

Use `grow` prop to control the flex-grow CSS property of an element.

<Scale
  hidePixels
  hidePreview
  valueLabel="Value"
  values={sprinkles.values.flexGrow}
/>

<Callout emoji="🚨">
  This page is a stub. Help us expand it by contributing!
</Callout>
