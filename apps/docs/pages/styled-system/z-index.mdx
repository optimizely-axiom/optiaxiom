import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Width

Use `z` prop to control the z-index of an element.

<Scale hidePixels hidePreview valueLabel="Value" values={tokens.zIndex} />

## Usage

<ax-demo name="z-index/usage" />
