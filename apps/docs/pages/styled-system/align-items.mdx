import { Scale } from "@/components/scale";
import { sprinkles } from "@optiaxiom/react";
import { Callout } from "nextra/components";

# Align Items

Use `alignItems` prop to control the align-items CSS property of an element.

<Scale
  hidePixels
  hidePreview
  valueLabel="Value"
  values={sprinkles.values.alignItems}
/>

<Callout emoji="🚨">
  This page is a stub. Help us expand it by contributing!
</Callout>
