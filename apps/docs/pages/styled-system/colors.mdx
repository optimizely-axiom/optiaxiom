import { ColorPalette } from "@/components/color-palette";

# Colors

`Box` component supports multiple props for controlling an element's background, border, and text colors.

You can also use the `--ax-colors-*` CSS variables directly in any CSS property.

## Basic usage

### Using style props

<ax-demo name="styles/prop-usage" />

### Using CSS variables

<ax-demo name="styles/css-usage" />

### Dark mode

Semantic color names such as `bg.information`, `bg.success`, etc. support light/dark modes out of the box.

Set the [color-scheme](https://developer.mozilla.org/en-US/docs/Web/CSS/color-scheme) property to `dark` preferably on the root `<html>` element to switch to dark mode.

```css /dark/
:root {
  color-scheme: dark;
}
```

## Color Tokens

<ax-color-tokens />

### Palette

<ColorPalette />
