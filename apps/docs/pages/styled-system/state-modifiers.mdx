import { sprinkles } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# State Modifiers

The `sx` prop supports various state modifiers to conditionally apply styles for different states like `hover`, `focus` etc.

<Scale
  hidePixels
  hidePreview
  valueLabel="Selector"
  values={sprinkles.modifiers}
/>

## Usage

<ax-demo name="styles-sx/hover-usage" />
