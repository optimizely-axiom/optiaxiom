import { Scale } from "@/components/scale";
import { sprinkles } from "@optiaxiom/react";

# Grid Column

Use `colSpan` prop to control the grid-column CSS property of an element.

<Scale
  hidePixels
  hidePreview
  valueLabel="Value"
  values={sprinkles.values.gridColumn}
/>

## Usage

<ax-demo meta='/colSpan="2"/' name="grid-column/span-usage" />
