import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Font Size

Use `fontSize` prop to control the font-size of an element.

<Scale hidePreview values={tokens.fontSize} />

## Documentation

### Usage

<ax-demo name="font/size-usage" />
