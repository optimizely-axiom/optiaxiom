import { tokens } from "@optiaxiom/globals";
import { Scale } from "@/components/scale";

import { Demo } from "@/components/demo";
import {
  NegativeUsage,
  MultipleUsage,
  SingleUsage,
  Usage,
} from "@/demos/margin";

# Margin

`Box` component supports multiple props for controlling an element's margin.

<Scale values={tokens.spacing} />

## Documentation

### Usage

Use the space scale to set the margin of elements.

<Demo component={Usage} />

### Multiple sides

<Demo component={MultipleUsage} />

### Single side

<Demo component={SingleUsage} />

### Negative

<Demo component={NegativeUsage} />
