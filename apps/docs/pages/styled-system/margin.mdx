import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Margin

`Box` component supports multiple props for controlling an element's margin.

<Scale values={tokens.spacing} />

## Basic usage

### Usage

Use the space scale to set the margin of elements.

<ax-demo name="margin/usage" />

### Multiple sides

<ax-demo name="margin/multiple-usage" />

### Single side

<ax-demo name="margin/single-usage" />

### Negative

<ax-demo name="margin/negative-usage" />
