import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Border Width

`Box` component supports the `border` prop for controlling the border width of an element.

<Scale hidePixels values={tokens.borderWidth} />

## Basic usage

### Usage

<ax-demo name="border-width/usage" />
