# Tokens

Axiom uses **CSS variables** to declare design tokens.

## Inline

You can directly grab the CSS variable name for the token from the `theme` object.

<ax-demo name="styles-inline-usage" />

## Props

For all root elements you can use the various style props to set the styles using tokens.

<ax-demo name="styles-prop-usage" />

## `theme`

This is the full theme object along with the values.

<ax-code file="packages/react/src/styles/tokens.ts" />
