<ax-layout />

Use `Stack` component to stack items vertically or horizontally.

## Usage

<ax-demo name="stack-usage" />

## Direction

Use `direction="row"` or `direction="horizontal"` to stack items horizontally:

<ax-demo name="stack-horizontal-usage" />

Use `direction="column"` or `direction="vertical"` to stack items vertically:

<ax-demo name="stack-vertical-usage" />
