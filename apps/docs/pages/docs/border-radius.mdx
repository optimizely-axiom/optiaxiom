import { tokens } from "@optiaxiom/react";
import { Scale } from "@/components/scale";

# Border Radius

`Box` component supports the `borderRadius` prop for controlling the border radius of an element.

<Scale values={tokens.radius} />

## Usage

<ax-demo name="border-radius-usage" />
