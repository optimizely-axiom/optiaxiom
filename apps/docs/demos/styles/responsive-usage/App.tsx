import { Box } from "@optiaxiom/react";

import { Canvas } from "../Canvas";

export function App() {
  return (
    <Canvas>
      <Box bg="bg.information.light" fontSize={["sm", "xl"]}>
        Contact
      </Box>
    </Canvas>
  );
}
