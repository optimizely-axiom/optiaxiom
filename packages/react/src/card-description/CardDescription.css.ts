import { recipe } from "../vanilla-extract";

export const description = recipe({
  base: [
    {
      color: "fg.tertiary",
      flex: "none",
      fontSize: "sm",
      w: "full",
    },
  ],
});
