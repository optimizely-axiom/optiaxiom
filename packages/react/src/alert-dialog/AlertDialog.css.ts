import { style } from "../vanilla-extract";

export const content = style({
  maxHeight: "90vh",
  maxWidth: "480px",
  minWidth: "350px",
  position: "absolute",
});

export const overlay = style({
  position: "absolute",
});
