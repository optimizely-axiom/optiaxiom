import { recipe } from "../vanilla-extract";

export const list = recipe({
  base: [
    {
      alignItems: "center",
      gap: "4",
      // justifyContent: "start",
      px: "xs",
      w: "full",
    },
  ],
});
