import { style } from "../vanilla-extract";

/**
 * Marker applied to the underlying `CardLink` anchor so an ancestor
 * `ProteusCard` can show its hover affordance only while the link is hovered.
 */
export const cardLinkMarker = style({});
