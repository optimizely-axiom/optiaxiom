---
"@optiaxiom/mcp": patch
---

Regenerated the bundled metadata and made its generation deterministic so the committed data stays in sync with `@optiaxiom/react` and `@optiaxiom/globals`. The `get_patterns` example `code` is now a list of `{ filename, content }` objects (in display order) instead of a filename-to-contents map.
