---
"@optiaxiom/react": patch
---

Clarify in `Menu`'s documentation that it is not a form control (no `value`/`onChange` or `Field` integration) and that there is currently no built-in filterable single-select form input. This helps when choosing between `Menu` (filterable) and `Select` (form control) for a single-select field.
