---
"@optiaxiom/web-components": patch
"@optiaxiom/react": patch
---

fix `Button` loading spinner being invisible on `primary` buttons in dark mode by inheriting the button text color, and add a `current` appearance to `Spinner` that follows `currentColor`
