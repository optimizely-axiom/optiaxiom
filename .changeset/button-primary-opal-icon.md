---
"@optiaxiom/web-components": patch
"@optiaxiom/react": patch
---

render the inverted icon box for `primary-opal` Button with a start-positioned icon, matching `primary`; end-positioned icons no longer render the inverted box
