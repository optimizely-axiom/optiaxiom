---
"@optiaxiom/mcp": patch
---

regenerated the embedded docs data so the `DateRangePickerTrigger` `formatRange` prop reports a clean `(startDate: Date, endDate: Date) => string` type instead of a truncated overloaded signature.
