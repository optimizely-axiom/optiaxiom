---
"@optiaxiom/globals": patch
"@optiaxiom/react": patch
"@optiaxiom/web-components": patch
"@optiaxiom/proteus": patch
"@optiaxiom/mcp": patch
---

Added the `bg.message.neutral` color token (light `#F5F6F7` / dark `#3C3D40`), usable via the `bg` sprinkle prop as `bg="message.neutral"`.
