---
"@optiaxiom/proteus": patch
---

Add a `ProteusCard` wrapper that shows a hover border only while a `CardLink`
inside the card is hovered (instead of a bare card hover), and use the
folded-corner document glyph for text/PDF/Word file icons.
